# Example .env for orchestrator
# Controls log level used by the application. Valid values: off, error, warn, info, debug, trace
RUST_LOG=info

# Optional: force log directory (relative to project root)
LOG_DIR=log
